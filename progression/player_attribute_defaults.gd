class_name PlayerAttributeDefaults extends Resource

## Related to lives though can expend to go out to retrieve items via the personnel copter system
@export_range(1, 100, 1) var personnel: int = 10
